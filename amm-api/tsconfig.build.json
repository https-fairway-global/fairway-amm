// Placeholder: Add tsconfig.build.json content for amm-api.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": [
    "src/**/*"
  ],
  "exclude": [
    "node_modules",
    "dist",
    "**/*.test.ts"
  ],
  "references": [
    { "path": "../amm-contract" }
    // Repeat signature-registry-api reference if added
    // { "path": "../signature-registry-api" }
  ]
} // Example Structure 